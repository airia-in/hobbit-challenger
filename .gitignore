.research/
docs/superpowers/

# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
.next
.astro
out
build
.vite
*.tsbuildinfo

# Turbo
.turbo

# Frontends staged for web-host aggregation
apps/web-host/sites

# Generated at build time from ANDROID_SHA256_CERT_FINGERPRINTS (scripts/generate-assetlinks.mjs)
apps/web/public/.well-known/assetlinks.json

# Env files
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea

# Test
coverage
playwright-report
test-results
.impeccable.md

# Orchestration scratch
.orchestrate

# Playwright CLI local artifacts
.playwright-cli/

# Capacitor staged web bundle (copied from apps/web/dist)
apps/mobile/www/

# TLS material for nginx compose (generate via scripts/nginx/generate-self-signed-certs.sh)
nginx/certs/*.pem

# Local data (SQLite DB + uploads)
data/*.db
data/*.db-journal
data/uploads/*
!data/uploads/.gitkeep

# Agent CLI per-run worktrees (concurrent sessions)
.agents/worktrees/
