name: Build and Publish

# Manual pipeline. Builds one image per deployable app — an app is deployable
# iff it has apps/<name>/Dockerfile — pushes them to GHCR tagged by git SHA
# (and :latest), then invokes the host-agnostic deploy dispatcher.
#
# All deployable images are rebuilt on every run (predictable over clever).
# Products that want affected-only builds can filter the discover matrix.

on:
  workflow_dispatch:

concurrency:
  group: build-publish-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  discover:
    name: Discover deployable apps
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      matrix: ${{ steps.set.outputs.matrix }}
      has_apps: ${{ steps.set.outputs.has_apps }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Discover apps with a Dockerfile
        id: set
        run: |
          apps=$(find apps -maxdepth 2 -name Dockerfile -printf '%h\n' | sed 's#apps/##' | sort)
          matrix=$(printf '%s\n' "$apps" | grep -v '^$' | jq -R . | jq -cs .)
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"
          if [ -n "$apps" ]; then
            echo "has_apps=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_apps=false" >> "$GITHUB_OUTPUT"
          fi
          echo "Deployable apps: ${apps:-none}"

  build:
    name: Build ${{ matrix.app }}
    needs: discover
    if: ${{ needs.discover.outputs.has_apps == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        app: ${{ fromJSON(needs.discover.outputs.matrix) }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Resolve image and tag
        id: image
        run: |
          registry="${{ vars.DEPLOY_REGISTRY }}"
          if [ -z "$registry" ]; then
            registry="ghcr.io/${GITHUB_REPOSITORY,,}"
          fi
          sha="${{ github.sha }}"
          echo "image=${registry}/${{ matrix.app }}" >> "$GITHUB_OUTPUT"
          echo "sha=${sha}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Require assetlinks fingerprints for web-host publish
        if: matrix.app == 'web-host'
        run: |
          if [ -z "${{ secrets.ANDROID_SHA256_CERT_FINGERPRINTS }}" ]; then
            echo "::error::ANDROID_SHA256_CERT_FINGERPRINTS secret is required to publish web-host."
            exit 1
          fi

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/${{ matrix.app }}/Dockerfile
          push: true
          tags: |
            ${{ steps.image.outputs.image }}:${{ steps.image.outputs.sha }}
            ${{ steps.image.outputs.image }}:latest
          build-args: |
            ANDROID_SHA256_CERT_FINGERPRINTS=${{ secrets.ANDROID_SHA256_CERT_FINGERPRINTS }}
            REQUIRE_ASSETLINKS_FINGERPRINTS=${{ matrix.app == 'web-host' && '1' || '0' }}
          cache-from: type=gha,scope=${{ matrix.app }}
          cache-to: type=gha,mode=max,scope=${{ matrix.app }}

  deploy:
    name: Deploy
    needs: [discover, build]
    if: ${{ needs.discover.outputs.has_apps == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0

      - name: Resolve deploy contract
        id: contract
        run: |
          registry="${{ vars.DEPLOY_REGISTRY }}"
          if [ -z "$registry" ]; then
            registry="ghcr.io/${GITHUB_REPOSITORY,,}"
          fi
          sha="${{ github.sha }}"
          apps='${{ needs.discover.outputs.matrix }}'
          images=$(echo "$apps" | jq -c --arg reg "$registry" --arg sha "$sha" \
            'map({ (.): ($reg + "/" + . + ":" + $sha) }) | add')
          {
            echo "DEPLOY_REGISTRY=${registry}"
            echo "DEPLOY_SHA=${sha}"
            echo "DEPLOY_APPS=$(echo "$apps" | jq -r 'join(",")')"
            echo "DEPLOY_IMAGES=${images}"
          } >> "$GITHUB_ENV"

      - name: Run deploy dispatcher
        env:
          DEPLOY_TARGET: ${{ vars.DEPLOY_TARGET }}
        run: node scripts/deploy/run.mjs
